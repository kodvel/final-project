# Contributing

This is an assignment repo, so keep process lightweight.

## Before opening a PR

Run the checks that match your change:

```bash
pnpm lint
pnpm test
pnpm build
```

## Guidelines

- Keep changes small and easy to review.
- Match existing folder conventions.
- Record important technical decisions in `docs/adr/`.
- Do not commit local environment files or secrets.
